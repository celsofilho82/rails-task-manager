<h1>Create new task</h1>

<%= form_for @task do |t|  %>
  <%= t.label :title %>
  <%= t.text_field :title %> <br>

  <%= t.label :details %>
  <%= t.text_field :details %><br>

  <%= t.label :completed %>
  <%= t.check_box(:completed) %>

  <%= t.submit %>

<% end %>
